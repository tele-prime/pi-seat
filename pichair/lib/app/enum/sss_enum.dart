enum ChairMenu { back, hand, seat, leg, foot }

enum ChairMovement {
  none,
  backForward,
  backBackward,
  handUpward,
  handDownward,
  seatForward,
  seatBackward,
  legUpward,
  legDownward,
  footForward,
  footBackward,
  tv
}

enum ChairImage { none, back, hand, seat, leg, foot }

enum TvLayout { apps, remote, touch, keyboard }

enum TvConnection { scanning, disconnected, connected }

enum TvDrawer { none, music, movie }