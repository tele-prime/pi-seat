import 'package:flutter/material.dart';

class SettingListDivider extends StatelessWidget {
  const SettingListDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      height: 40,
      thickness: 1,

    );
  }
}
